meta {
  name: Secured admin
  type: http
  seq: 4
}

get {
  url: {{connectionPath}}/examples/secured/admin
  body: none
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}

assert {
  res.status: eq 200
}
